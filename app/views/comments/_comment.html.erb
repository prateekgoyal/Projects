<p>
  <b><%= comment.commenter %></b> <%= comment.body %>
</p>
 
<p>
	<% if session[:user_id] %>
		<% if session[:user_id] == @post.user_id  || session[:user_id] == comment.user_id%>
  			<%= link_to 'Delete Comment', [comment.post, comment],
               :confirm => 'Are you sure?',
               :method => :delete %>
    	<% end %>
    <% end %>
</p>